Repository hôte pour la formation Vue Expert.

La bibliothèque Vue est intégrée en submodule (`submodule/vue-lib-submodule-kit`) et suit une approche Atomic Design.

Pour cloner avec la lib : `git clone --recurse-submodules <url>` (sinon `git submodule update --init --recursive`).
