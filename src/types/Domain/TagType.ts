// Modèle front utilisé par le store
export type TagType = {
  id: string; // identifiant front (documentId)
  title: string;
  color: string;
  createdAt: string;
  updatedAt: string;
  publishedAt: string;
};

